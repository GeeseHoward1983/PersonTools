<UserControl x:Class="PersonalTools.UserControls.RsaEncryptionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonalTools.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Margin="10" AllowDrop="True" Drop="RsaTab_Drop" PreviewDragOver="Grid_PreviewDragOver">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="待加密文本/待验签数据" Margin="0,0,0,5">
            <TextBox Name="RsaInput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
        </GroupBox>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                <TextBlock Text="输入类型:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <RadioButton Name="RsaInputStringRadio" Content="字符串" GroupName="RsaInputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <RadioButton Name="RsaInputHexRadio" Content="Hex" GroupName="RsaInputType" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <TextBlock Text="公钥:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Name="RsaPublicKey" Width="300" Margin="0,0,5,0" VerticalAlignment="Center"/>

                <TextBlock Text="私钥:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Name="RsaPrivateKey" Width="300" Margin="0,0,5,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                <TextBlock Text="密钥长度:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Name="RsaKeySizeComboBox" Width="120" Margin="0,0,20,0" VerticalAlignment="Center" DisplayMemberPath="Name"/>

                <Button Content="加密" Click="RsaEncrypt_Click" Padding="15,5" Margin="5,0"/>
                <Button Content="解密" Click="RsaDecrypt_Click" Padding="15,5" Margin="0,0,5,0"/>
                <Button Content="签名" Click="RsaSign_Click" Padding="15,5" Margin="0,0,5,0"/>
                <Button Content="验签" Click="RsaVerify_Click" Padding="15,5" Margin="0,0,5,0"/>
                <Button Content="生成密钥对" Click="RsaGenerateKeyPair_Click" Padding="15,5" Margin="0,0,5,0"/>
                <Button Content="导入公钥" Click="RsaImportPublicKey_Click" Padding="15,5" Margin="0,0,5,0"/>
                <Button Content="导入私钥" Click="RsaImportPrivateKey_Click" Padding="15,5" Margin="0,0,5,0"/>
                <Button Content="清空" Click="RsaClear_Click" Padding="15,5" Margin="0,0,5,0"/>
            </StackPanel>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="加密结果/签名结果" Margin="0,5,0,0">
            <TextBox Name="RsaResult" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
        </GroupBox>
    </Grid>
</UserControl>