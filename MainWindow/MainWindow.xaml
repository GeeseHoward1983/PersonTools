<Window x:Class="MyTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyTool"
        mc:Ignorable="d"
        Title="常用工具集" Height="900" Width="1440" AllowDrop="True" WindowStartupLocation="CenterScreen" FontFamily="SimSun">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 主内容区域 -->
        <TabControl Grid.Row="1" Margin="5,5,5,10">
            <!-- PE文件分析器标签页 -->
            <TabItem Header="PE文件分析器">
                <Grid AllowDrop="True" Drop="MainWindow_Drop">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 菜单栏 -->
                    <Menu Grid.Row="0">
                        <MenuItem Header="文件">
                            <MenuItem Header="打开" Click="OpenFile_Click"/>
                            <Separator/>
                            <MenuItem Header="退出" Click="Exit_Click"/>
                        </MenuItem>
                    </Menu>

                    <!-- PE分析内容 -->
                    <TabControl Grid.Row="1" Margin="5">
                        <!-- 文件信息和图标标签页 -->
                        <TabItem Header="文件信息">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" MinHeight="350"/>
                                    <RowDefinition Height="250" MinHeight="150"/>
                                </Grid.RowDefinitions>

                                <!-- 上方：PE头基本信息和附加信息 -->
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="300"/>
                                        <ColumnDefinition Width="*" MinWidth="300"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 左侧：PE头基本信息 -->
                                    <GroupBox Grid.Column="0" Header="PE头信息" Margin="5">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <StackPanel Name="HeaderInfoPanel" Margin="5"/>
                                        </ScrollViewer>
                                    </GroupBox>

                                    <!-- 右侧：附加信息 -->
                                    <GroupBox Grid.Column="1" Header="附加信息" Margin="5">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <StackPanel Name="AdditionalInfoPanel" Margin="5"/>
                                        </ScrollViewer>
                                    </GroupBox>
                                </Grid>

                                <!-- 下方：图标信息 -->
                                <GroupBox Grid.Row="1" Header="图标信息" Margin="5">
                                    <DataGrid Name="IconsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Header="图标" Width="80">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Image Source="{Binding ImageSource}" Width="64" Height="64" Stretch="Uniform"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTextColumn Header="宽度" Binding="{Binding Width}" Width="80"/>
                                            <DataGridTextColumn Header="高度" Binding="{Binding Height}" Width="80"/>
                                            <DataGridTextColumn Header="色深" Binding="{Binding BitsPerPixel}" Width="80"/>
                                            <DataGridTextColumn Header="大小(字节)" Binding="{Binding Size}" Width="*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- 依赖和函数信息标签页 -->
                        <TabItem Header="依赖和函数">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" MinWidth="200"/>
                                    <ColumnDefinition Width="2*" MinWidth="400"/>
                                </Grid.ColumnDefinitions>

                                <!-- 左侧：依赖信息 -->
                                <GroupBox Grid.Column="0" Header="动态库依赖" Margin="5">
                                    <TreeView Name="DependencyTree" MouseDoubleClick="DependencyTree_MouseDoubleClick"/>
                                </GroupBox>

                                <!-- 右侧：导入和导出函数 -->
                                <Grid Grid.Column="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" MinHeight="200"/>
                                        <RowDefinition Height="*" MinHeight="200"/>
                                    </Grid.RowDefinitions>

                                    <!-- 右上：导入函数 -->
                                    <GroupBox Grid.Row="0" Header="导入函数列表" Margin="5">
                                        <DataGrid Name="ImportFunctionsGrid" AutoGenerateColumns="False" IsReadOnly="True">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="DLL名称" Binding="{Binding DllName}" Width="150"/>
                                                <DataGridTextColumn Header="函数名称" Binding="{Binding FunctionName}" Width="*"/>
                                                <DataGridTextColumn Header="序号" Binding="{Binding OrdinalDisplay}" Width="120">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="导入类型" Binding="{Binding IsOrdinalImport}" Width="80"/>
                                                <DataGridTextColumn Header="延迟加载" Binding="{Binding IsDelayLoaded}" Width="80"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </GroupBox>

                                    <!-- 右下：导出函数 -->
                                    <GroupBox Grid.Row="1" Header="导出函数列表" Margin="5">
                                        <DataGrid Name="ExportFunctionsGrid" AutoGenerateColumns="False" IsReadOnly="True">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="函数名称" Binding="{Binding Name}" Width="*"/>
                                                <DataGridTextColumn Header="序号" Binding="{Binding OrdinalDisplay}" Width="120">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="RVA" Binding="{Binding RVA, StringFormat=0x\{0:X8\}}" Width="100"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </GroupBox>
                                </Grid>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>

            <!-- ELF文件分析器标签页 -->
            <TabItem Header="ELF文件分析器" AllowDrop="True" Drop="ELFAnalyzerTab_Drop">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 菜单栏 -->
                    <Menu Grid.Row="0">
                        <MenuItem Header="文件">
                            <MenuItem Header="打开" Click="OpenELFFile_Click"/>
                        </MenuItem>
                    </Menu>

                    <!-- ELF分析内容 -->
                    <TabControl Grid.Row="1" Margin="5,5,5,5">
                        <!-- ELF头信息标签页 -->
                        <TabItem Header="ELF头信息">
                            <GroupBox Header="ELF Header" Margin="5">
                                <ScrollViewer>
                                    <TextBlock Name="ELFHeaderInfoTextBlock" TextWrapping="Wrap" Padding="10"/>
                                </ScrollViewer>
                            </GroupBox>
                        </TabItem>

                        <!-- 程序头信息标签页 -->
                        <TabItem Header="程序头信息">
                            <GroupBox Header="程序头" Margin="5">
                                <DataGrid Name="ELFProgramHeaderDataGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Margin="5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="*"/>
                                        <DataGridTextColumn Header="偏移" Binding="{Binding Offset}" Width="*"/>
                                        <DataGridTextColumn Header="虚拟地址" Binding="{Binding VirtAddr}" Width="*"/>
                                        <DataGridTextColumn Header="物理地址" Binding="{Binding PhysAddr}" Width="*"/>
                                        <DataGridTextColumn Header="文件大小" Binding="{Binding FileSize}" Width="*"/>
                                        <DataGridTextColumn Header="内存大小" Binding="{Binding MemSize}" Width="*"/>
                                        <DataGridTextColumn Header="标志" Binding="{Binding Flags}" Width="*"/>
                                        <DataGridTextColumn Header="对齐" Binding="{Binding Align}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </GroupBox>
                        </TabItem>

                        <!-- 节头信息标签页 -->
                        <TabItem Header="节头信息">
                            <GroupBox Header="节头" Margin="5">
                                <DataGrid Name="ELFSectionHeaderDataGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Margin="5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="序号" Binding="{Binding Index}" Width="60"/>
                                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="*"/>
                                        <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="*"/>
                                        <DataGridTextColumn Header="偏移" Binding="{Binding Offset}" Width="*"/>
                                        <DataGridTextColumn Header="大小" Binding="{Binding Size}" Width="*"/>
                                        <DataGridTextColumn Header="整体大小" Binding="{Binding EntSize}" Width="*"/>
                                        <DataGridTextColumn Header="标志" Binding="{Binding Flags}" Width="*"/>
                                        <DataGridTextColumn Header="链接" Binding="{Binding Link}" Width="60"/>
                                        <DataGridTextColumn Header="信息" Binding="{Binding Info}" Width="60"/>
                                        <DataGridTextColumn Header="对齐" Binding="{Binding Align}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </GroupBox>
                        </TabItem>

                        <!-- 节到段映射信息标签页 -->
                        <TabItem Header="节到段映射">
                            <GroupBox Header="Section to Segment Mapping" Margin="5">
                                <ScrollViewer>
                                    <TextBlock Name="ELFSectionToSegmentInfoTextBlock" TextWrapping="Wrap" Padding="10"/>
                                </ScrollViewer>
                            </GroupBox>
                        </TabItem>

                        <!-- 符号表信息标签页 -->
                        <TabItem Header="符号表信息">
                            <GroupBox Header="符号表" Margin="5">
                                <DataGrid Name="ELFSymbolTableDataGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Margin="5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="序号" Binding="{Binding Number}" Width="60"/>
                                        <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="*"/>
                                        <DataGridTextColumn Header="大小" Binding="{Binding Size}" Width="80"/>
                                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="*"/>
                                        <DataGridTextColumn Header="绑定" Binding="{Binding Bind}" Width="*"/>
                                        <DataGridTextColumn Header="可见性" Binding="{Binding Vis}" Width="*"/>
                                        <DataGridTextColumn Header="索引" Binding="{Binding Ndx}" Width="60"/>
                                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="2*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </GroupBox>
                        </TabItem>

                        <!-- 动态段信息标签页 -->
                        <TabItem Header="动态段信息">
                            <GroupBox Header="动态段" Margin="5">
                                <DataGrid Name="ELFDynamicSectionDataGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Margin="5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="标签" Binding="{Binding Tag}" Width="*"/>
                                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="2*"/>
                                        <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="2*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </GroupBox>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>

            <!-- Mach-O文件分析器标签页 -->
            <TabItem Header="Mach-O文件分析器">
                <Grid>
                    <TextBlock Text="Mach-O文件分析器功能待实现" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                </Grid>
            </TabItem>

            <!-- 常用错误码对应错误信息查看器标签页 -->
            <TabItem Header="常用错误码对应错误信息查看器">
                <Grid>
                    <TabControl Margin="5">
                        <!-- Windows errno查询 -->
                        <TabItem Header="Windows errno">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="WindowsErrnoInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="WindowsErrnoQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="WindowsErrnoResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- Linux errno查询 -->
                        <TabItem Header="Linux errno">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="LinuxErrnoInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="LinuxErrnoQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="LinuxErrnoResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- Mac errno查询 -->
                        <TabItem Header="Mac errno">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="MacErrnoInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="MacErrnoQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="MacErrnoResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- HTTP状态码查询 -->
                        <TabItem Header="HTTP状态码">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="HttpStatusCodeInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="HttpStatusCodeQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="状态信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="HttpStatusCodeResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- SQL Server错误码查询 -->
                        <TabItem Header="SQL Server">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="SqlServerErrorInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="SqlServerErrorQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="SqlServerErrorResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- MySQL错误码查询 -->
                        <TabItem Header="MySQL">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="MySqlErrorInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="MySqlErrorQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="MySqlErrorResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- Oracle SQLCODE查询 -->
                        <TabItem Header="Oracle">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="OracleSqlCodeInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="OracleSqlCodeQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="OracleSqlCodeResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- ODBC错误码查询 -->
                        <TabItem Header="ODBC">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="OdbcErrorInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="OdbcErrorQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="OdbcErrorResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>

                        <!-- Windows系统错误码查询 -->
                        <TabItem Header="Windows系统错误码">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox Name="WindowsSystemErrorInput" Width="100" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                    <Button Content="查询" Click="WindowsSystemErrorQuery_Click" Padding="10,0"/>
                                </StackPanel>

                                <GroupBox Grid.Row="1" Header="错误信息" Padding="10">
                                    <ScrollViewer>
                                        <TextBlock Name="WindowsSystemErrorResult" TextWrapping="Wrap" />
                                    </ScrollViewer>
                                </GroupBox>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>


            <!-- HASH值计算标签页 -->
            <TabItem Header="HASH值计算">
                <ScrollViewer Margin="10" VerticalScrollBarVisibility="Auto" AllowDrop="True" Drop="HashTab_Drop" PreviewDragOver="Grid_PreviewDragOver">
                    <StackPanel>
                        <!-- 第一行：MD5和SHA1并排显示 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- MD5计算 -->
                            <GroupBox Grid.Column="0" Header="MD5计算" Margin="0,0,5,0">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="输入要计算MD5的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBox Name="MD5InputTextBox" Height="25" VerticalAlignment="Center" Width="513"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                        <RadioButton Name="MD5StringInputRadio" Content="普通字符串" GroupName="MD5InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <RadioButton Name="MD5HexInputRadio" Content="Hex字符串" GroupName="MD5InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <Button Content="计算MD5" Click="CalculateMD5_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <Button Content="清空" Click="ClearMD5_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="MD5结果：" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <Label Grid.Column="1" Name="MD5ResultLabel" Content="等待计算..." Padding="5" 
                                               Background="White" BorderBrush="Gray" BorderThickness="1" 
                                               HorizontalContentAlignment="Left"/>
                                    </Grid>
                                </Grid>
                            </GroupBox>

                            <!-- SHA1计算 -->
                            <GroupBox Grid.Column="1" Header="SHA1计算" Margin="5,0,0,0">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="输入要计算SHA1的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBox Name="SHA1InputTextBox" Height="25" VerticalAlignment="Center" Width="513"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                        <RadioButton Name="SHA1StringInputRadio" Content="普通字符串" GroupName="SHA1InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <RadioButton Name="SHA1HexInputRadio" Content="Hex字符串" GroupName="SHA1InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <Button Content="计算SHA1" Click="CalculateSHA1_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <Button Content="清空" Click="ClearSHA1_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="SHA1结果：" Margin="0,0,5,0" VerticalAlignment="Top"/>
                                        <Label Grid.Column="1" Name="SHA1ResultLabel" Content="等待计算..." Padding="5" 
                                               Background="White" BorderBrush="Gray" BorderThickness="1" 
                                               HorizontalContentAlignment="Left"/>
                                    </Grid>
                                </Grid>
                            </GroupBox>
                        </Grid>

                        <!-- 第二行：SHA224和SHA256并排显示 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- SHA224计算 -->
                            <GroupBox Grid.Column="0" Header="SHA224计算" Margin="0,0,5,0">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="输入要计算SHA224的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBox Name="SHA224InputTextBox" Height="25" VerticalAlignment="Center" Width="499"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                        <RadioButton Name="SHA224StringInputRadio" Content="普通字符串" GroupName="SHA224InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <RadioButton Name="SHA224HexInputRadio" Content="Hex字符串" GroupName="SHA224InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <Button Content="计算SHA224" Click="CalculateSHA224_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <Button Content="清空" Click="ClearSHA224_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="SHA224结果：" Margin="0,0,5,0" VerticalAlignment="Top"/>
                                        <Label Grid.Column="1" Name="SHA224ResultLabel" Content="等待计算..." Padding="5" 
                                           Background="White" BorderBrush="Gray" BorderThickness="1" 
                                           HorizontalContentAlignment="Left"/>
                                    </Grid>
                                </Grid>
                            </GroupBox>

                            <!-- SHA256计算 -->
                            <GroupBox Grid.Column="1" Header="SHA256计算" Margin="5,0,0,0">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="输入要计算SHA256的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBox Name="SHA256InputTextBox" Width="497" Height="25" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                        <RadioButton Name="SHA256StringInputRadio" Content="普通字符串" GroupName="SHA256InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <RadioButton Name="SHA256HexInputRadio" Content="Hex字符串" GroupName="SHA256InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <Button Content="计算SHA256" Click="CalculateSHA256_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <Button Content="清空" Click="ClearSHA256_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="SHA256结果：" Margin="0,0,5,0" VerticalAlignment="Top"/>
                                        <Label Grid.Column="1" Name="SHA256ResultLabel" Content="等待计算..." Padding="5" 
                                           Background="White" BorderBrush="Gray" BorderThickness="1" 
                                           HorizontalContentAlignment="Left"/>
                                    </Grid>
                                </Grid>
                            </GroupBox>
                        </Grid>

                        <!-- SHA384计算 - 独占一行 -->
                        <GroupBox Header="SHA384计算" Margin="0,0,0,10" MinWidth="600">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="输入要计算SHA384的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <TextBox Name="SHA384InputTextBox" Width="1186" Height="25" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                    <RadioButton Name="SHA384StringInputRadio" Content="普通字符串" GroupName="SHA384InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="SHA384HexInputRadio" Content="Hex字符串" GroupName="SHA384InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <Button Content="计算SHA384" Click="CalculateSHA384_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <Button Content="清空" Click="ClearSHA384_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="SHA384结果：" Margin="0,0,5,0" VerticalAlignment="Top"/>
                                    <Label Grid.Column="1" Name="SHA384ResultLabel" Content="等待计算..." Padding="5" 
                                           Background="White" BorderBrush="Gray" BorderThickness="1" 
                                           HorizontalContentAlignment="Left"/>
                                </Grid>
                            </Grid>
                        </GroupBox>

                        <!-- SHA512计算 - 独占一行 -->
                        <GroupBox Header="SHA512计算" Margin="0,0,0,10" MinWidth="600">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="输入要计算SHA512的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <TextBox Name="SHA512InputTextBox" Width="1184" Height="25" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                    <RadioButton Name="SHA512StringInputRadio" Content="普通字符串" GroupName="SHA512InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="SHA512HexInputRadio" Content="Hex字符串" GroupName="SHA512InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <Button Content="计算SHA512" Click="CalculateSHA512_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <Button Content="清空" Click="ClearSHA512_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="SHA512结果：" Margin="0,0,5,0" VerticalAlignment="Top"/>
                                    <Label Grid.Column="1" Name="SHA512ResultLabel" Content="等待计算..." Padding="5" 
                                           Background="White" BorderBrush="Gray" BorderThickness="1" 
                                           HorizontalContentAlignment="Left"/>
                                </Grid>
                            </Grid>
                        </GroupBox>

                        <!-- SHA3计算 - 独占一行 -->
                        <GroupBox Header="SHA3计算" Margin="0,0,0,10" MinWidth="600">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="输入要计算SHA3的值：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <TextBox Name="SHA3InputTextBox" Width="1195" Height="25" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                    <RadioButton Name="SHA3StringInputRadio" Content="普通字符串" GroupName="SHA3InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="SHA3HexInputRadio" Content="Hex字符串" GroupName="SHA3InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <ComboBox Name="SHA3AlgorithmComboBox" Width="200" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <Button Content="计算SHA3" Click="CalculateSHA3_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <Button Content="清空" Click="ClearSHA3_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="SHA3结果：" Margin="0,0,5,0" VerticalAlignment="Top"/>
                                    <Label Grid.Column="1" Name="SHA3ResultLabel" Content="等待计算..." Padding="5" 
                                           Background="White" BorderBrush="Gray" BorderThickness="1" 
                                           HorizontalContentAlignment="Left"/>
                                </Grid>
                            </Grid>
                        </GroupBox>

                        <TextBlock Text="将文件拖放到此标签页以计算哈希值" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center" 
                                   FontSize="14" 
                                   Foreground="Gray" 
                                   Margin="0,20,0,0" 
                                   Name="FileDropHint"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- CRC计算标签页 -->
            <TabItem Header="CRC计算">
                <Grid Margin="10" AllowDrop="True" Drop="CrcTab_Drop" PreviewDragOver="Grid_PreviewDragOver">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="待计算文本" Margin="0,0,0,5">
                        <TextBox Name="CRCInputTextBox" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <RadioButton Name="CRCStringInputRadio" Content="普通字符串" GroupName="CRCInputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <RadioButton Name="CRCHexInputRadio" Content="Hex字符串" GroupName="CRCInputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <ComboBox Name="CRCAlgorithmComboBox" Width="200" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <Button Content="计算CRC" Click="CalculateCRC_Click" Padding="10,3" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <Button Content="清空" Click="ClearCRC_Click" Padding="10,3" Margin="0,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <GroupBox Grid.Row="2" Header="计算结果" Margin="0,5,0,0">
                        <Label Name="CRCResultLabel" Content="等待计算..." Padding="5" Background="White" BorderBrush="Gray" BorderThickness="1" VerticalContentAlignment="Top" MinHeight="30"/>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Base64编解码标签页 -->
            <TabItem Header="Base64编解码">
                <Grid Margin="10" AllowDrop="True" Drop="Base64Tab_Drop" PreviewDragOver="Grid_PreviewDragOver">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="待编码文本" Margin="0,0,0,5">
                        <TextBox Name="Base64Input" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <RadioButton Name="Base64StringInputRadio" Content="普通字符串" GroupName="Base64InputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <RadioButton Name="Base64HexInputRadio" Content="Hex字符串" GroupName="Base64InputType" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <Button Content="编码" Click="Base64Encode_Click" Padding="15,5" Margin="5"/>
                        <Button Content="解码" Click="Base64Decode_Click" Padding="15,5" Margin="5"/>
                        <Button Content="清空" Click="Base64Clear_Click" Padding="15,5" Margin="5"/>
                    </StackPanel>

                    <GroupBox Grid.Row="2" Header="编码结果/待解码文本" Margin="0,5,0,0">
                        <TextBox Name="Base64Result" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- URL编解码标签页 -->
            <TabItem Header="URL编解码">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="待编码文本" Margin="0,0,0,5">
                        <TextBox Name="UrlInput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <Button Content="编码" Click="UrlEncode_Click" Padding="15,5" Margin="5"/>
                        <Button Content="解码" Click="UrlDecode_Click" Padding="15,5" Margin="5"/>
                        <Button Content="清空" Click="UrlClear_Click" Padding="15,5" Margin="5"/>
                    </StackPanel>

                    <GroupBox Grid.Row="2" Header="编码结果/待解码文本" Margin="0,5,0,0">
                        <TextBox Name="UrlResult" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- AES加解密标签页 -->
            <TabItem Header="AES加解密">
                <Grid Margin="10" AllowDrop="True" Drop="AesTab_Drop" PreviewDragOver="Grid_PreviewDragOver">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="待加密文本" Margin="0,0,0,5">
                        <TextBox Name="AesInput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>

                    <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <TextBlock Text="输入类型:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <RadioButton Name="AesInputStringRadio" Content="字符串" GroupName="AesInputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Name="AesInputHexRadio" Content="Hex" GroupName="AesInputType" Margin="0,0,20,0" VerticalAlignment="Center"/>

                            <TextBlock Text="密钥:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Name="AesKey" Width="120" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <RadioButton Name="AesKeyStringRadio" Content="字符串" GroupName="AesKeyInputType" IsChecked="True" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <RadioButton Name="AesKeyHexRadio" Content="Hex" GroupName="AesKeyInputType" Margin="0,0,20,0" VerticalAlignment="Center"/>

                            <TextBlock Text="IV:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Name="AesIV" Width="100" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <RadioButton Name="AesIVStringRadio" Content="字符串" GroupName="AesIVInputType" IsChecked="True" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <RadioButton Name="AesIVHexRadio" Content="Hex" GroupName="AesIVInputType" Margin="0,0,20,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <TextBlock Text="模式:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox Name="AesModeComboBox" Width="80" Margin="0,0,20,0" VerticalAlignment="Center" DisplayMemberPath="Name"/>

                            <TextBlock Text="填充:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox Name="AesPaddingComboBox" Width="130" Margin="0,0,20,0" VerticalAlignment="Center" DisplayMemberPath="Name"/>

                            <Button Content="加密" Click="AesEncrypt_Click" Padding="15,5" Margin="5,0"/>
                            <Button Content="解密" Click="AesDecrypt_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="清空" Click="AesClear_Click" Padding="15,5" Margin="0,0,5,0"/>
                        </StackPanel>
                    </StackPanel>

                    <GroupBox Grid.Row="3" Header="加密结果/待解密文本" Margin="0,5,0,0">
                        <TextBox Name="AesResult" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- RSA加解密标签页 -->
            <TabItem Header="RSA加解密">
                <Grid Margin="10" AllowDrop="True" Drop="RsaTab_Drop" PreviewDragOver="Grid_PreviewDragOver">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="待加密文本/待验签数据" Margin="0,0,0,5">
                        <TextBox Name="RsaInput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>

                    <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <TextBlock Text="输入类型:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <RadioButton Name="RsaInputStringRadio" Content="字符串" GroupName="RsaInputType" IsChecked="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <RadioButton Name="RsaInputHexRadio" Content="Hex" GroupName="RsaInputType" Margin="0,0,20,0" VerticalAlignment="Center"/>

                            <TextBlock Text="公钥:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Name="RsaPublicKey" Width="300" Margin="0,0,5,0" VerticalAlignment="Center"/>

                            <TextBlock Text="私钥:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Name="RsaPrivateKey" Width="300" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <TextBlock Text="密钥长度:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox Name="RsaKeySizeComboBox" Width="120" Margin="0,0,20,0" VerticalAlignment="Center" DisplayMemberPath="Name"/>

                            <Button Content="加密" Click="RsaEncrypt_Click" Padding="15,5" Margin="5,0"/>
                            <Button Content="解密" Click="RsaDecrypt_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="签名" Click="RsaSign_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="验签" Click="RsaVerify_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="生成密钥对" Click="RsaGenerateKeyPair_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="导入公钥" Click="RsaImportPublicKey_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="导入私钥" Click="RsaImportPrivateKey_Click" Padding="15,5" Margin="0,0,5,0"/>
                            <Button Content="清空" Click="RsaClear_Click" Padding="15,5" Margin="0,0,5,0"/>
                        </StackPanel>
                    </StackPanel>

                    <GroupBox Grid.Row="3" Header="加密结果/签名结果" Margin="0,5,0,0">
                        <TextBox Name="RsaResult" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>
                    </GroupBox>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>